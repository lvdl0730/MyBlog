import type { MockArticleItem } from "./mockArticles";

const articleMarkdownMap: Record<number, string> = {
  1: `# 前言

这篇记录聚焦在博客后台布局的第一阶段，目标不是一次把所有页面做完，而是先把结构、层级和交互边界搭起来。

## 先把骨架搭稳

布局阶段最重要的是把 Header、Sider、Content 的职责分清楚，让路由切换、固定导航和内容滚动各自独立。

- 统一 BasicLayout 容器和内容边距
- 让顶部栏和侧边栏固定，不跟随内容区滚动
- 给搜索弹层和面包屑预留稳定位置

## 导航细节怎么收口

顶部导航不只是把几个入口摆出来，更关键的是 hover、激活态和收起展开时的反馈要统一。

后面扩展写文章、标签页、搜索页时，如果头部交互已经稳定，就不需要反复返工。

## 内容区需要留出扩展空间

内容区不能只为首页服务，还要支撑时间轴、详情页、表单页等差异很大的页面结构。

所以这一层更偏向基础设施，而不是单页视觉。

## 总结

布局阶段做得越稳，后面的页面开发就越像往现成框架里填内容，而不是边写边返工。`,
  2: `# 路由设计的起点

前端路由结构决定了页面层级、面包屑、导航状态和未来的权限边界，这一步提前设计能减少很多后期返工。

## 先分清页面层级

首页、文章列表、文章详情、计划页、写文章页虽然都在一个站点里，但它们的职责完全不同。

- 首页负责聚合内容和入口
- 文章列表负责检索和归档
- 详情页负责阅读和目录跳转
- 编辑页负责创作和草稿信息

## 用 meta 驱动页面外壳

breadcrumb、标题和部分页面级操作，适合由路由 meta 提供，而不是散落在各个页面里。

这样可以让布局组件统一读取信息，页面本身只关心业务内容。

## 给未来页面留接口

路由结构不应该只服务当前页面，还要考虑写文章、标签详情、用户中心和搜索结果页。

预先留出规范化路径，能避免后面再重命名一整批链接。`,
  3: `# 为什么列表页要先做好

文章列表页的目标不是把文章平铺出来，而是让用户快速建立时间感、主题感和最近更新的认知。

## 为什么用时间轴

博客天然带有时间维度，时间轴比普通卡片列表更适合展示写作积累的过程感。

当文章数量增加后，按月归档也更容易扫描和回顾。

## 列表页不只是列表

文章列表页同时承担搜索、概览和最新更新的职责，所以顶部需要搜索栏和指标卡来辅助浏览。

- 搜索标题、摘要和标签
- 展示当前结果数和标签覆盖范围
- 保留管理员写文章入口

## 为详情页做铺垫

列表页里的日期、阅读时长、标签这些信息，既是摘要，也是引导用户决定要不要进入详情。

因此卡片信息要克制，但必须有判断价值。`,
};

const buildFallbackMarkdown = (article: MockArticleItem) => `# 前言

${article.summary}

## 主题背景

这篇内容围绕“${article.title}”展开，当前仍以结构化 mock 内容为主，重点是先把详情页和编辑页能力搭起来。

## 当前实现重点

- 详情页需要承载长文本阅读
- 目录需要从 Markdown 标题自动提取
- 编辑页需要围绕同一份 Markdown 内容做预填

## 下一步

后续可以把这部分替换成真实 Markdown 内容，并接入后端文章详情接口。`;

export const getArticleMarkdown = (article: MockArticleItem) =>
  articleMarkdownMap[article.id] ?? buildFallbackMarkdown(article);

export const buildEditorContent = (article: MockArticleItem) => getArticleMarkdown(article);
