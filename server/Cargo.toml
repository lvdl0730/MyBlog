[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
salvo = { version = "0.72", features = ["cors", "logging"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dotenvy = "0.15"